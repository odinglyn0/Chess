{
  "name": "chess-gantry-tooling",
  "version": "0.1.0",
  "private": true,
  "description": "Formatting and git-hook tooling for the chess-gantry project",
  "scripts": {
    "format": "npm run format:prettier && npm run format:python",
    "format:check": "npm run format:prettier:check && npm run format:python:check",
    "format:prettier": "node node_modules/prettier/bin/prettier.cjs --write .",
    "format:prettier:check": "node node_modules/prettier/bin/prettier.cjs --check .",
    "format:python": "uv run python scripts/strip_comments.py --write src tests examples scripts && uv run python -m black .",
    "format:python:check": "uv run python -m black --check . && uv run python scripts/strip_comments.py --check src tests examples scripts",
    "steering:check": "uv run python scripts/steering_check.py .",
    "check": "npm run format:check && npm run steering:check",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "prettier": "^3.4.2",
    "prettier-plugin-sh": "^0.14.0",
    "prettier-plugin-toml": "^2.0.1"
  }
}
